#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(0); cin.tie(0);
    int n, m, k;
    cin >> n >> m >> k;
    cout << n << " " << m << " " << k << "\n";
    vector<int> a(n), b(m);
    for (int i = 0; i < n; i++) cin >> a[i];
    cout << 0 << " ";
    return 0;
}